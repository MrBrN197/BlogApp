<section>
  <div class="user">
    <%= render @user %>
    <p><%= @user.bio %></p>
  </div>
  <div class="posts">
    <% @user.posts.each do |post|  %>
      <%= render post %>
    <% end %>
  </div>
</section>