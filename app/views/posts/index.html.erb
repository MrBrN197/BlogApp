<section>
  <div class="user">
    <%= render @user %>
  </div>

  <div class="posts">
    <% @posts.each do |post| %>
      <%= render post %>
      <%= render post.comments %>
    <% end %>
  </div>
  <button>See All Posts</button>
</section>